class Solution {
    public int fib(int n) {
        if( n == 1 || n==0){
            return n;
        } 
        int nm1 =  fib(n - 1);
        int nm2 =  fib(n - 2);
        int fibo = nm1 + nm2;
        return fibo;
    }
}
